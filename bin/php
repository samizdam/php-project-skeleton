#!/bin/bash

ENV_FILE=${ENV_FILE:-.env}

source ${ENV_FILE}

docker run --rm --interactive \
    --init \
    --user `id -u`:`id -g` \
    --volume $PWD:$WORKDIR \
    --env-file ${ENV_FILE} \
    ${PHP_DEV_IMAGE}:${REVISION} php "$@"

